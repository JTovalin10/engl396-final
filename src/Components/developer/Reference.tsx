export default function Reference() {
  return (
    <article className="endpoint-card">
      <h3>Reference</h3>
      <p>The end user reference documentation provides a high-level overview of every function in the application, including its parameters, return values, and example calls. It is the fastest way to look up what a function expects and what it returns.</p>
      <p>The source code for each helper file contains the full implementation. Reading the source alongside the reference documentation is the most effective way to understand how a function works before modifying it.</p>
    </article>
  )
}
